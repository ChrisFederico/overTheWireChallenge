# The password for the next level is stored in the file data.txt next to the word millionth

Solution:

This one is a piece of cake, just pipe the grep command after the cat command on the data.txt file using "millionth" as
argument for grep and you're done.

~$ cat data.txt | grep millionth
